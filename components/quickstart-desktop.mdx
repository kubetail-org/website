import { Tabs } from 'nextra/components';

<Tabs items={['Package Managers', 'Shell', 'Binaries']}>
  <Tabs.Tab>
    First, install the Kubetail CLI tool (`kubetail`) via your favorite package manager:

    ```sh
    # Homebrew
    brew install kubetail

    # Krew
    kubectl krew install kubetail

    # Snap
    sudo snap install kubetail

    # Winget
    winget install Kubetail.Kubetail

    # Chocolatey
    choco install kubetail

    # Scoop
    scoop install kubetail

    # asdf
    asdf plugin add kubetail https://github.com/kubetail-org/asdf-kubetail.git
    asdf install kubetail latest

    # Arch Linux (AUR)
    git clone https://aur.archlinux.org/kubetail-bin.git
    cd kubetail-bin && makepkg -si
    ```

    Next, start the web dashboard using the `serve` subcommand:

    ```sh
    kubetail serve
    ```

    This command will open [http://localhost:7500/](http://localhost:7500/) in your default browser. You can also view logs directly in your terminal using the `logs` subcommand:

    ```sh
    kubetail logs frontend:deployments/web --tail=10 --follow
    ```

    Have fun tailing your logs!
  </Tabs.Tab>
  <Tabs.Tab>
    First, download and run the [install.sh](/install.sh) script:

    ```sh
    curl -sS https://www.kubetail.com/install.sh | bash
    ```

    Next, start the web dashboard using the `serve` subcommand:

    ```sh
    kubetail serve
    ```

    This command will open [http://localhost:7500/](http://localhost:7500/) in your default browser. You can also view logs directly in your terminal using the `logs` subcommand:

    ```sh
    kubetail logs frontend:deployments/web --tail=10 --follow
    ```

    Have fun tailing your logs!
  </Tabs.Tab>
  <Tabs.Tab>
    Download the binary for your OS/Arch (from the latest [release binaries](https://github.com/kubetail-org/kubetail/releases/latest)):

    * [Darwin/amd64](https://github.com/kubetail-org/kubetail/releases/latest/download/kubetail-darwin-amd64)
    * [Darwin/arm64](https://github.com/kubetail-org/kubetail/releases/latest/download/kubetail-darwin-arm64)
    * [Linux/amd64](https://github.com/kubetail-org/kubetail/releases/latest/download/kubetail-linux-amd64)
    * [Linux/arm64](https://github.com/kubetail-org/kubetail/releases/latest/download/kubetail-linux-arm64)
    * [Windows/amd64](https://github.com/kubetail-org/kubetail/releases/latest/download/kubetail-windows-amd64)

    Rename the file and make it executable:

    ```sh
    mv <filename> kubetail
    chmod a+x kubetail
    ```

    Next, start the web dashboard using the `serve` subcommand:

    ```sh
    kubetail serve
    ```

    This command will open [http://localhost:7500/](http://localhost:7500/) in your default browser. You can also view logs directly in your terminal using the `logs` subcommand:

    ```sh
    kubetail logs frontend:deployments/web --tail=10 --follow
    ```

    Have fun tailing your logs!
  </Tabs.Tab>
</Tabs>
