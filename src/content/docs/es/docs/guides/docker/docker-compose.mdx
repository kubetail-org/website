---
title: Docker Compose
description: "Use Docker Compose con la imagen `kubetail-cli` para montar su kubeconfig e iniciar el servidor del dashboard de Kubetail en localhost:7500."
---

import { Aside } from '@astrojs/starlight/components';

Para usar la herramienta CLI localmente con `docker-compose`, puede usar este archivo de configuracion [docker-compose.yml](https://raw.githubusercontent.com/kubetail-org/kubetail/refs/heads/main/config/examples/docker-compose.yml) como punto de partida:

```sh
curl -LO https://raw.githubusercontent.com/kubetail-org/kubetail/refs/heads/main/config/examples/docker-compose.yml
docker-compose up
```

Esto montara su archivo local `.kube/config` en un contenedor `kubetail-cli` y pondra en marcha el servidor del dashboard en [http://localhost:7500](http://localhost:7500).

<Aside type="tip">
Tambien puede usar la imagen Docker `kubetail-cli` con:

* [Docker Run](/es/docs/guides/docker/docker-run)
* [Kubernetes Pod](/es/docs/guides/docker/kubernetes-pod)
</Aside>
