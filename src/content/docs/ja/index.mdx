---
title: ""
description: "Kubetail は Kubernetes 向けのリアルタイムログダッシュボードです。コンテナのログをターミナルまたはブラウザーで表示できます。デスクトップ、クラスター、Docker のどこでも実行できます。"
template: splash
hero:
  title: 使いやすい Kubernetes ロギング
  tagline: Kubetail は Kubernetes 向けのリアルタイムログダッシュボードです
  actions:
    - text: ドキュメントを見る
      link: /ja/docs/concepts/introduction
      icon: right-arrow
    - text: GitHub で見る
      link: https://github.com/kubetail-org/kubetail
      icon: external
      variant: minimal
  image:
    html: |
      <div class="flex flex-col items-center gap-2 w-full">
        <video class="w-full max-w-screen-sm rounded-md dark:hidden" poster="https://assets.kubetail.com/demo-light-poster.bb7f6d4a.webp" controls>
          <source src="https://assets.kubetail.com/demo-light.48aba4ab.mp4" type="video/mp4" />
        </video>
        <video class="w-full max-w-screen-sm rounded-md hidden dark:block" poster="https://assets.kubetail.com/demo-dark-poster.6070fba2.webp" controls>
          <source src="https://assets.kubetail.com/demo-dark.251c0575.mp4" type="video/mp4" />
        </video>
        <a class="underline hover:no-underline text-(--sl-color-accent)" href="https://www.kubetail.com/demo">
          ライブデモ
          <svg class="inline align-baseline" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewBox="0 0 24 24" height="16">
            <path d="M7 17L17 7"></path>
            <path d="M7 7h10v10"></path>
          </svg>
        </a>
      </div>
head:
  - tag: title
    content: "Kubetail | Kubernetes ログダッシュボード"
  - tag: link
    attrs:
      rel: preload
      as: image
      fetchpriority: high
      href: 'https://assets.kubetail.com/demo-light-poster.bb7f6d4a.webp'
  - tag: link
    attrs:
      rel: preload
      as: image
      fetchpriority: high
      href: 'https://assets.kubetail.com/demo-dark-poster.6070fba2.webp'
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

## クイックスタート

`kubetail` CLI ツールは、デスクトップで Kubetail を使うための主要なエントリーポイントです。インストールには、お好みのパッケージマネージャーを使用できます。

<Tabs syncKey="os">
  <TabItem label="macOS">
    **Homebrew**
    ```sh
    brew install kubetail
    ```

    **Krew**
    ```sh
    kubectl krew install kubetail
    ```

    **MacPorts**
    ```sh
    sudo port install kubetail
    ```

    **Nix (Flake)**
    ```sh
    nix profile add github:kubetail-org/kubetail-nix
    ```

    **Nix (Classic)**
    ```sh
    nix-env -i -f https://github.com/kubetail-org/kubetail-nix/archive/refs/heads/main.tar.gz
    ```

    **asdf**
    ```sh
    asdf plugin add kubetail https://github.com/kubetail-org/asdf-kubetail.git
    asdf install kubetail latest
    ```
  </TabItem>
  <TabItem label="Linux">
    **Homebrew**
    ```sh
    brew install kubetail
    ```

    **Krew**
    ```sh
    kubectl krew install kubetail
    ```

    **Snap**
    ```sh
    sudo snap install kubetail
    ```

    **Ubuntu/Mint (apt)**
    ```sh
    sudo add-apt-repository ppa:kubetail/kubetail
    sudo apt update && sudo apt install kubetail-cli
    ```

    **Fedora/CentOS/RHEL/Amazonlinux/Mageia (copr)**
    ```sh
    dnf copr enable kubetail/kubetail
    dnf install kubetail
    ```

    **SUSE (zypper)**
    ```sh
    zypper addrepo 'https://download.opensuse.org/repositories/home:/kubetail/$releasever/' kubetail
    zypper refresh && zypper install kubetail-cli
    ```

    **Alpine (apk)**
    ```sh
    apk add kubetail --repository=https://dl-cdn.alpinelinux.org/alpine/edge/testing
    ```

    **Arch (AUR)**
    ```sh
    yay -S --noconfirm kubetail-cli
    ```

    **Gentoo (GURU)**
    ```sh
    ACCEPT_KEYWORDS="~$(portageq envvar ARCH)" emerge dev-util/kubetail
    ```

    **Nix (Flake)**
    ```sh
    nix profile add github:kubetail-org/kubetail-nix
    ```

    **Nix (Classic)**
    ```sh
    nix-env -i -f https://github.com/kubetail-org/kubetail-nix/archive/refs/heads/main.tar.gz
    ```

    **asdf**
    ```sh
    asdf plugin add kubetail https://github.com/kubetail-org/asdf-kubetail.git
    asdf install kubetail latest
    ```
  </TabItem>
  <TabItem label="Windows">
    **Winget**
    ```sh
    winget install kubetail
    ```

    **Krew**
    ```sh
    krew install kubetail
    ```

    **Chocolatey**
    ```sh
    choco install kubetail
    ```

    **Scoop**
    ```sh
    scoop install kubetail
    ```
  </TabItem>
</Tabs>

<br />

---

リリース済みの[バイナリ](https://github.com/kubetail-org/kubetail/releases/latest)をダウンロードするか、インストールスクリプトを使うこともできます。

```sh
curl -sS https://www.kubetail.com/install.sh | bash
```

<br />
<br />

<Aside type="tip" title="次のステップ">
**1. Web ダッシュボード（GUI）を起動する**

```sh
kubetail serve
```

**2. ターミナルでログを表示する**

```sh
kubetail logs -f deployments/my-app
```

**3. 高度な機能を有効にする（Kubetail API をインストールする）**

```sh
kubetail cluster install
```

**4. ローカル設定ファイルを初期化する**

```sh
kubetail config init
```

[コマンド](/ja/docs/reference/cli) の一覧はドキュメントを参照してください。ログの tail をお楽しみください。
</Aside>
