import { Tabs } from 'nextra/components';

import QuickstartCluster from '@/components/quickstart-cluster.mdx';
import QuickstartDesktop from '@/components/quickstart-desktop.mdx';

## Key Features

* Clean, easy-to-use interface
* View your log messages in real-time
* Filter logs by:
  * Workload (e.g. Deployment, CronJob, StatefulSet)
  * Absolute or relative time range
  * Node properties (e.g. availability zone, CPU architecture, node ID)
  * Search messages using grep
* Use web dashboard or CLI tool
* Web dashboard can be installed on desktop or in cluster
* Uses your Kubernetes API to retrieve log messages (private by default)
* Open source (Apache-2.0 license)

## Quickstart (Desktop)

<QuickstartDesktop />

## Quickstart (Cluster)

<QuickstartCluster />
