# `kubetail cluster upgrade`

```console
Subcommands for interacting with Kubetail's charts repository in Helm.

The Kubetail charts repository is located at:
https://kubetail-org.github.io/helm-charts/

Usage:
  kubetail cluster repo [command]

Available Commands:
  add         Add the repository
  remove      Remove the repository
  update      Update the repository

Flags:
  -h, --help   help for repo

Use "kubetail cluster repo [command] --help" for more information about a command.
```
