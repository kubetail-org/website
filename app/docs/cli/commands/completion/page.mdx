# `kubetail completion`

```console
Generate the autocompletion script for kubetail for the specified shell.
See each sub-command's help for details on how to use the generated script.

Usage:
  kubetail completion [command]

Available Commands:
  bash        Generate the autocompletion script for bash
  fish        Generate the autocompletion script for fish
  powershell  Generate the autocompletion script for powershell
  zsh         Generate the autocompletion script for zsh

Flags:
  -h, --help   help for completion

Use "kubetail completion [command] --help" for more information about a command.
```

## Subcommands

* [`kubetail completion bash`](/docs/cli/completion/commands/bash)
* [`kubetail completion fish`](/docs/cli/completion/commands/fish)
* [`kubetail completion powershell`](/docs/cli/completion/commands/powershell)
* [`kubetail completion zsh`](/docs/cli/completion/commands/zsh)
